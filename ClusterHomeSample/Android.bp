android_app {
    name: "ClusterHomeSample",

    // Only compile source java files in this apk.
    srcs: ["src/**/*.java"],

    libs: [
        "android.car",
    ],

    platform_apis: true,
    certificate: "platform",
    privileged: true,
}
